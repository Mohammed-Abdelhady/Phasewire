hyperflow-role: worker

| Field | Value |
|---|---|
| Status | Pending |
| Task | T4 |
| Specialist | Debugger |
| Depends on | Integration prerequisite |

# Split workflow lifecycle coverage

## Task

Replace the monolithic CLI lifecycle test with focused reducer-valid integration cases and shared explicit setup.

## Why

The current case repeatedly performs filesystem and replay work and exceeds Vitest's default Windows budget while obscuring which transition failed.

## Scope

**IN:** test setup helpers and focused cases for execution, blocking review/remediation, fresh review, validation, and readiness.

**OUT:** production commands/reducer behavior, sleeps, platform skips, retries, and timeout increases.

## Files in scope

- **Read:** `packages/cli/src/commands/workflows.ts`, `packages/cli/src/commands/progress.ts`, server facade transition contracts.
- **Modify:** `packages/cli/src/progress.test.ts`.
- **Create:** none.

## Acceptance criteria

- Focused cases retain command-level coverage for the full lifecycle.
- Shared setup creates explicit reducer-valid starting states without duplicating business rules.
- Blocking review opens remediation; approved remediation resolves the finding.
- A fresh clear review plus passed required validation derives readiness.
- Each case stays within Vitest's default budget on Windows.

## Test cases

| # | Scenario | Input or setup | Expected behavior | Notes |
|---|---|---|---|---|
| 1 | Execution completion | Approved plan, execute, complete-execution | Projection is ready for independent review | First lifecycle segment |
| 2 | Blocking review | Blocking finding, then complete-review | Remediation required; readiness false | Product invariant |
| 3 | Approved remediation | Plan, approve, start, and complete remediation | Finding records a resolution event | Resolution semantics |
| 4 | Fresh evidence | New clear review plus passed `lint` validation | Deployment readiness is true | Final derivation |
| 5 | Integration | Run focused file repeatedly on Windows CI | Passes within default timeout with no skip/sleep | Original failure proof |

## Related context

Finding 7; durable event semantics remain authoritative.

## Context

`progress.test.ts` drives real CLI commands against a temporary `PhasewireCoreFacade`; one case currently covers the full event lifecycle. The reducer remains authoritative, so shared setup must create valid events rather than mock or bypass transitions.

## Gotchas

Do not replace command coverage with direct facade mutations for the behavior each case claims to prove; setup may use the facade only to establish explicit valid pre-state.

## Constraints

Only modify listed files. No production changes, test weakening, timeout increases, hook bypass, or actor attribution. Keep the file at most 350 lines, preserve cleanup, and return without narrative padding.

## Security constraints

Use temporary roots only and never persist raw commands, prompts, or runtime identifiers. Do not read `.env*`, certificates, keys, SSH/cloud credentials, or run destructive recursive deletion, `sudo`, `chmod 777`, force-push, or package publication. Report `BLOCKED:` if blocked access is required.

## Output format

Return exactly one: **Completed** with one line per change plus optional future-task notes; `OVERSIZE: <reason>` with a `SUGGESTED-SPLIT:` list; `CONSULT: <peer> — <question>` with at most five context lines; or `BLOCKED: <reason>`.
