hyperflow-role: worker

| Field | Value |
|---|---|
| Status | Pending |
| Task | T10 |
| Specialist | Writer |
| Depends on | T3, T5, T6, T9 |

# Correct the documented quality-gate contract

## Task

Align contributor documentation and the PR template with final executable quality scripts and the local-first review authorization contract.

## Why

AGENTS and README claim `format:check` is enforced by aggregate gates even though scripts omit it, and final prose must reflect the settled Node, CI, and review behavior.

## Scope

**IN:** Node requirement, quality command coverage, formatting status, review authorization language, PR checklist, and final script truth.

**OUT:** script changes, formatting the repository, public command/state redesign, or new policy beyond T3/T6/T9.

## Files in scope

- **Read:** final `package.json`, `.github/workflows/ci.yml`, generated review projections — establish executable and projected truth.
- **Modify:** `AGENTS.md`, `README.md`, `docs/quality-gates.md`, `docs/code-review.md`, `.github/PULL_REQUEST_TEMPLATE.md` — synchronize contributor-facing policy.
- **Create:** none.

## Acceptance criteria

- No document claims an aggregate script runs an absent command.
- `format` and `format:check` are contributor hygiene until made blocking.
- Node minimum, commit gates, push gates, browser preparation, and audit match executable scripts.
- Review-write authorization remains explicit, target-bound, batched, and local-first.
- No public behavior is invented beyond completed source tasks.

## Test cases

| # | Scenario | Input or setup | Expected behavior | Notes |
|---|---|---|---|---|
| 1 | Gate matrix | Compare every documented aggregate gate with `package.json` | Each listed command exists in the actual chain | Truthfulness |
| 2 | Formatting search | Search enforcement wording | Only opt-in contributor-hygiene wording remains | Original finding |
| 3 | Node search | Compare README, manifest, lock, and CI | All state 22.13 minimum while CI also uses Node 24 | Compatibility contract |
| 4 | Review authorization | Compare doctrine, guide, template, and generated review files | Plain review is local; posting is explicit, bound, and batched | Security contract |
| 5 | E2E | N/A — documentation-only projection | Source behavior is already exercised by T3, T6, and T9 | Deliberate omission |

## Related context

Finding 4; T10 runs last so prose records settled behavior. T3 owns policy source, T5 generated projections, T6 Node evidence, and T9 executable quality composition.

## Context

The repository treats `AGENTS.md`, README, review guides, quality-gate docs, and the PR template as contributor contracts. They must describe commands and authorization effects precisely without widening public functionality.

## Gotchas

Describe actual script composition after T9; do not copy the original claims or imply that formatting became blocking.

## Constraints

Only modify listed files. Do not change scripts or generated adapters, bypass hooks, or name a tool as repository actor. Keep every Markdown file at most 350 lines and return without narrative padding.

## Security constraints

Do not include tokens, raw prompts, logs, ports, PIDs, browser state, or secret-bearing examples. Do not read `.env*`, certificates, keys, SSH/cloud credentials, or run destructive recursive deletion, `sudo`, `chmod 777`, force-push, package publication, or external posting. Report `BLOCKED:` if blocked access is required.

## Output format

Return exactly one: **Completed** with one line per change plus optional future-task notes; `OVERSIZE: <reason>` with a `SUGGESTED-SPLIT:` list; `CONSULT: <peer> — <question>` with at most five context lines; or `BLOCKED: <reason>`.
